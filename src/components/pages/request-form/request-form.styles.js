import { makeStyles } from '@material-ui/styles';

export default makeStyles(theme => ({
  paper: {
    padding: theme.spacing(3, 2)
  },
  fullWidth: {
    width: '100%'
  },
  padding12: {
    padding: 12
  },
  buttonFix: {
    textAlign: 'center',
  }
}));
