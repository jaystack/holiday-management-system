import { connect } from 'react-redux';

import RequestHoliday from './request-holiday.component';

const mapStateToProps = () => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(RequestHoliday);
