import { connect } from 'react-redux';

import SignUpExample from './signup-example.component';

const mapStateToProps = () => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(SignUpExample);
