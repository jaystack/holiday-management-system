import { connect } from 'react-redux';

import TextWithButton from './text-with-button.component';

const mapStateToProps = () => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(TextWithButton);
